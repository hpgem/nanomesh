import os
import sys
__version__ = '0.1.0'

# To update the package version number, edit CITATION.cff
# citationfile = os.path.join(sys.exec_prefix, 'citation/nanomesh', 'CITATION.cff')
# with open(citationfile, 'r') as cff:
#     for line in cff:
#         if 'version:' in line:
#             __version__ = line.replace('version:', '').strip().strip('"')
